<!-- <?xml version="1.0" encoding="UTF-8"?>
<configuration>

    콘솔에 로그 출력
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
    </appender>

    로그 레벨 설정 (info 이상의 로그 출력)
    <root level="info">
        <appender-ref ref="console"/>
    </root>

</configuration> -->